version: "3.3"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # If you want to mount local images during dev, uncomment:
    # volumes:
    #   - ./public/books:/app/public/books
